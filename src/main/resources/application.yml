spring:
  application:
    name: SpringBootPractice

  datasource:
    url: jdbc:postgresql://localhost:5431/practicedb
    username: bikash
    password: 1999
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

